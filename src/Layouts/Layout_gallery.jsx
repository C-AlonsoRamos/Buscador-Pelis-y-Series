import './Layout-gallery.css';

import React from 'react';

const Layout_gallery = ({ children }) => {
  return <section className="pt-section">{children}</section>;
};

export default Layout_gallery;
